# baselinewalk

Ambient audio segment harmonizer and baseline comparison CLI tool.

## Installation

```bash
pip install .
```

## Usage

```bash
baselinewalk harmonize [options]
```

This command runs the `main()` function defined in `baselinewalk.py`.
